import React from 'react';
import NavBar from './top-nav';

export default function Header(props) {
    return (
        <header>
            <NavBar />
            <h1>Hot || Cold</h1>
        </header>
    );
}

